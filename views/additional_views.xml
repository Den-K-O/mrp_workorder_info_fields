<?xml version="1.0" encoding="utf-8" ?>
<data>
    <record id="view_mrp_workorder_tree_remaining_days" model="ir.ui.view">
        <field name="name">mrp.workorder.tree.remove.remaining.days</field>
        <field name="model">mrp.workorder</field>
        <field name="inherit_id" ref="mrp.mrp_production_workorder_tree_editable_view" />
        <field name="arch" type="xml">
            <field name="date_planned_start" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date_planned_start and date_planned_start &lt; current_date</attribute>
            </field>
            <field name="date_planned_finished" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date_planned_finished and date_planned_finished &lt; current_date</attribute>
            </field>
        </field>
    </record>

    <record id="view_mrp_production_tree_remaining_days" model="ir.ui.view">
        <field name="name">mrp.production.tree.remove.remaining.days</field>
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_tree_view" />
        <field name="arch" type="xml">
            <field name="date_planned_start" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date_planned_start and date_planned_start &lt; current_date</attribute>
            </field>
            <field name="date_deadline" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date_deadline and date_deadline &lt; current_date</attribute>
            </field>
        </field>
    </record>

    <record id="view_stock_move_form_remaining_days" model="ir.ui.view">
        <field name="name">stock.move.form.remove.remaining.days</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_form" />
        <field name="arch" type="xml">
            <field name="date" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date and date &lt; current_date</attribute>
            </field>
            <field name="date_deadline" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date_deadline and date_deadline &lt; current_date</attribute>
            </field>
        </field>
    </record>

    <record id="view_stock_picking_tree_remaining_days" model="ir.ui.view">
        <field name="name">stock.picking.tree.remove.remaining.days</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree" />
        <field name="arch" type="xml">
            <field name="scheduled_date" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">scheduled_date and scheduled_date &lt; current_date</attribute>
            </field>
            <field name="date_deadline" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date_deadline and date_deadline &lt; current_date</attribute>
            </field>
        </field>
    </record>

    <record id="view_stock_move_tree_receipt_remaining_days" model="ir.ui.view">
        <field name="name">stock.move.tree.receipt.remove.remaining.days</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_tree_receipt_picking" />
        <field name="arch" type="xml">
            <field name="date" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date and date &lt; current_date</attribute>
            </field>
            <field name="date_deadline" position="attributes">
                <attribute name="widget"></attribute>
                <attribute name="decoration-danger">date_deadline and date_deadline &lt; current_date</attribute>
            </field>
        </field>
    </record>
</data>
